meta {
  name: Create Todo
  type: http
  seq: 2
}

post {
  url: {{host}}/api/todos
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "title": "My todo auth to delete",
    "notes": "Notes about my todo item",
    "dueDate": "2026-04-21"
  }
}
